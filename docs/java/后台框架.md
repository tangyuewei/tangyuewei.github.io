---
prev: ./开发常用工具类.md
next: ./服务器.md
meta:
  - name: description
    content: 努力只是一种生活方式，先敬业，再乐业
  - name: keywords
    content: 唐悦玮,tk.mybatis,mybatis-plus,java后台框架
---
# 后台框架

## Spring

### IOC

### AOP

### SpringMVC

## Mybatis

>`tk.mybatis`与`mybatis-plus` 是在 MyBatis 框架的基础上提供了很多工具，让开发更加高效

### tk.mybatis
- 引入依赖
```yaml
<dependency>
    <groupId>tk.mybatis</groupId>
    <artifactId>mapper-spring-boot-starter</artifactId>
    <version>2.0.2</version>
</dependency>
```
- 配置 application.yml
```yaml
mybatis:
    type-aliases-package: 实体类的存放路径，如：com.tangyuewei.spring.boot.entity
    mapper-locations: classpath:mapper/*.xml
```
- 创建通用的父级接口
```java
import tk.mybatis.mapper.common.Mapper;
import tk.mybatis.mapper.common.MySqlMapper;

/**
 * 自己的 Mapper
 * 特别注意，该接口不能被扫描到，否则会出错
 * <p>Title: MyMapper</p>
 * <p>Description: </p>
 *
 * @author Webster
 * @version 1.0.0
 * @date 2019/6/29 10:15
 */
public interface MyMapper<T> extends Mapper<T>, MySqlMapper<T> {
}
```
### mybatis-plus
- 引入依赖
```yaml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>3.3.1.tmp</version>
</dependency>
```
- 配置 `MapperScan` 注解

```java
@SpringBootApplication
@MapperScan("com.tangyuewei.mybatisplus.mapper")
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

}
```
## Shiro 

